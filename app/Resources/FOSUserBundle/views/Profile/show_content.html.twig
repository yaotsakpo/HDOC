{% trans_default_domain 'FOSUserBundle' %}

{% extends 'base.html.twig' %}

{% block main %}

<div class="fos_user_user_show" style="right: -70%">
    <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
</div>

{% endblock %}